= bootstrap_form_tag url: "/auth/identity/register" do |f|
  = f.hidden_field :name, value: name
  = f.hidden_field :email, value: email
  = f.password_field :password
  = f.password_field :password_confirmation
  .actions
    = f.primary "Save password"
